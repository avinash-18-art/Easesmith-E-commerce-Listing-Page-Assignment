import React from 'react';

const Pagination = () => {
  return (
    <div className="pagination">
      <button>Previous</button>
      <button>Next</button>
    </div>
  );
};

export default Pagination;
